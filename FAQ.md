# ❓ Часто задаваемые вопросы (FAQ)

## 🆕 О новых возможностях

### В: Что нового добавлено в систему?

**О:** Добавлены две новые возможности:
1. **Цена инструмента** - можно указывать стоимость каждого инструмента
2. **Категории инструментов** - можно группировать инструменты (например: "Электроинструмент", "Ручной инструмент")

Обе возможности необязательные - существующие инструменты продолжат работать без изменений.

---

### В: Это обратно совместимо?

**О:** Да, полностью! Все существующие инструменты:
- ✅ Останутся в базе данных
- ✅ Продолжат работать без изменений
- ✅ Будут иметь `price = null` (не отображается)
- ✅ Будут иметь `categoryId = null` (без категории)

Вы можете постепенно добавлять цены и категории к существующим инструментам.

---

### В: Обязательно ли указывать цену и категорию?

**О:** Нет, оба поля необязательные:
- Цена (`price`) - может быть пустой
- Категория (`categoryId`) - может быть не выбрана

Инструмент можно создать без цены и категории, как раньше.

---

## 🔄 Применение изменений

### В: Как быстро применить изменения?

**О:** Процесс занимает ~5 минут:

```bash
# 1. Резервная копия (ВАЖНО!)
docker exec toolmanager_postgres pg_dump -U postgres toolmanager > backup.sql

# 2. Обновить зависимости
cd backend && npm install
cd ../frontend && pnpm install

# 3. Применить миграцию
cd ../backend
npx prisma migrate deploy
npx prisma generate

# 4. Запустить
npm run dev  # backend
cd ../frontend && pnpm dev  # frontend
```

Подробнее: [QUICK_START.md](./QUICK_START.md)

---

### В: Что делать, если что-то пошло не так?

**О:** У вас есть резервная копия! Восстановите её:

```bash
# Восстановить базу данных
docker exec -i toolmanager_postgres psql -U postgres toolmanager < backup.sql

# Откатить код
git checkout HEAD~1
```

Или см. раздел "Откат изменений" в [MIGRATION_GUIDE.md](./MIGRATION_GUIDE.md)

---

### В: Нужно ли останавливать систему для обновления?

**О:** Да, на время миграции (1-2 минуты):
1. Остановите backend и frontend
2. Примените миграцию БД
3. Запустите обратно

Для production рекомендуется:
- Выполнять в нерабочее время
- Использовать blue-green deployment
- Протестировать на staging окружении

---

## 📊 Категории

### В: Как создать категории?

**О:** Два способа:

**1. Через UI (рекомендуется):**
- Пока нет отдельной страницы управления категориями
- Категории создаются через API (см. ниже)

**2. Через API:**
```bash
curl -X POST http://localhost:5000/api/categories \
  -H "Content-Type: application/json" \
  -b cookies.txt \
  -d '{"name": "Электроинструмент"}'
```

См. полную документацию: [API_CATEGORIES.md](./API_CATEGORIES.md)

---

### В: Сколько категорий можно создать?

**О:** Неограниченно! Но рекомендуется 5-15 категорий для удобства:
- Электроинструмент
- Ручной инструмент
- Измерительный
- Садовый инструмент
- Пневматический
- И т.д.

---

### В: Можно ли переименовать категорию?

**О:** Да, через API:

```bash
curl -X PUT http://localhost:5000/api/categories/CATEGORY_ID \
  -H "Content-Type: application/json" \
  -b cookies.txt \
  -d '{"name": "Новое название"}'
```

Все инструменты в этой категории останутся в ней.

---

### В: Что произойдет с инструментами при удалении категории?

**О:** Инструменты **НЕ УДАЛЯЮТСЯ**!
- Категория удаляется
- У инструментов `categoryId` становится `null`
- Инструменты становятся "без категории"
- Можно назначить им другую категорию

Это безопасное поведение (onDelete: SetNull).

---

### В: Категории видны другим компаниям?

**О:** Нет! Категории изолированы по компаниям:
- Каждая компания имеет свои категории
- Нельзя увидеть или использовать категории других компаний
- Уникальность имени проверяется только в рамках вашей компании

---

## 💰 Цены

### В: В какой валюте хранится цена?

**О:** Цена хранится как число (Decimal), без указания валюты. 

На фронтенде отображается с символом ₽ (рубль), но вы можете изменить это:

```javascript
// frontend/src/components/ToolCard.jsx
<Text>{parseFloat(tool.price).toFixed(2)} ₽</Text>

// Можно изменить на $, €, или любую другую валюту
<Text>${parseFloat(tool.price).toFixed(2)}</Text>
```

---

### В: Почему используется Decimal, а не Float?

**О:** Decimal обеспечивает точность для денежных расчетов:

```javascript
// Float (ПЛОХО для денег)
0.1 + 0.2 = 0.30000000000000004 ❌

// Decimal (ХОРОШО для денег)
0.1 + 0.2 = 0.30 ✅
```

В PostgreSQL Decimal хранится как NUMERIC с точностью.

---

### В: Как отобразить цену на фронтенде?

**О:** Используйте parseFloat():

```javascript
// Правильно ✅
const price = parseFloat(tool.price).toFixed(2);

// С проверкой на null ✅
const price = tool.price ? parseFloat(tool.price).toFixed(2) : '—';

// Неправильно ❌
const price = tool.price.toFixed(2); // Ошибка если Decimal объект
```

---

### В: Можно ли считать общую стоимость инструментов?

**О:** Да! Примеры:

**SQL запрос:**
```sql
SELECT SUM(price) as total_value
FROM "Tool"
WHERE "companyId" = 'YOUR_COMPANY_ID';
```

**JavaScript (на фронтенде):**
```javascript
const totalValue = tools
  .filter(t => t.price)
  .reduce((sum, tool) => sum + parseFloat(tool.price), 0);

console.log(`Общая стоимость: ${totalValue.toFixed(2)} ₽`);
```

**Через Prisma:**
```javascript
const result = await prisma.tool.aggregate({
  where: { companyId: 'YOUR_COMPANY_ID' },
  _sum: { price: true }
});
console.log(result._sum.price);
```

---

## 🔐 Права доступа

### В: Кто может создавать категории?

**О:** Пользователи с правом `TOOL_CREATE`. Обычно это:
- Босс (все права)
- Менеджеры склада
- Роли с правом управления инструментами

---

### В: Можно ли настроить отдельные права для категорий?

**О:** Сейчас используются те же права, что и для инструментов:
- `TOOL_CREATE` - создание категорий
- `TOOL_READ` - просмотр категорий
- `TOOL_UPDATE` - редактирование категорий
- `TOOL_DELETE` - удаление категорий

Если нужны отдельные права (CATEGORY_*), можно добавить в Prisma enum Permission.

---

## 🐛 Проблемы и решения

### В: Backend не запускается - ошибка bcrypt

**О:** Переустановите зависимости:

```bash
cd backend
rm -rf node_modules package-lock.json
npm install
npm rebuild bcrypt
```

---

### В: Категории не загружаются на фронте

**О:** Проверьте:

1. **Backend запущен?**
   ```bash
   curl http://localhost:5000/api/health
   ```

2. **Есть права TOOL_READ?**
   Откройте консоль браузера (F12) → Network → проверьте ответ API

3. **API доступен?**
   ```bash
   curl http://localhost:5000/api/categories -b cookies.txt
   ```

---

### В: Ошибка "Cannot read property 'toFixed' of undefined"

**О:** Проверьте, что цена существует перед форматированием:

```javascript
// Неправильно ❌
{tool.price.toFixed(2)}

// Правильно ✅
{tool.price && parseFloat(tool.price).toFixed(2)}

// Или с запасным значением ✅
{tool.price ? parseFloat(tool.price).toFixed(2) : '—'}
```

---

### В: Миграция не применяется

**О:** Проверьте:

1. **База данных запущена?**
   ```bash
   docker ps | grep toolmanager_postgres
   ```

2. **Правильные credentials?**
   Проверьте `.env` файл

3. **Сбросьте миграцию и примените заново:**
   ```bash
   npx prisma migrate reset
   npx prisma migrate dev
   ```

---

## 📈 Отчеты и статистика

### В: Как получить статистику по категориям?

**О:** Примеры запросов:

**1. Количество инструментов в каждой категории:**
```sql
SELECT 
  c.name,
  COUNT(t.id) as count
FROM "Category" c
LEFT JOIN "Tool" t ON t."categoryId" = c.id
WHERE c."companyId" = 'YOUR_COMPANY_ID'
GROUP BY c.id, c.name;
```

**2. Общая стоимость по категориям:**
```sql
SELECT 
  c.name,
  COUNT(t.id) as tools_count,
  SUM(t.price) as total_value
FROM "Category" c
LEFT JOIN "Tool" t ON t."categoryId" = c.id
WHERE c."companyId" = 'YOUR_COMPANY_ID'
GROUP BY c.id, c.name
ORDER BY total_value DESC;
```

**3. Самые дорогие инструменты:**
```sql
SELECT name, price, (
  SELECT name FROM "Category" WHERE id = "Tool"."categoryId"
) as category
FROM "Tool"
WHERE "companyId" = 'YOUR_COMPANY_ID'
AND price IS NOT NULL
ORDER BY price DESC
LIMIT 10;
```

---

### В: Можно ли экспортировать данные с ценами?

**О:** Да! Несколько способов:

**1. Через Prisma Studio:**
```bash
npx prisma studio
# Выберите таблицу Tool → Export
```

**2. CSV экспорт через SQL:**
```sql
COPY (
  SELECT 
    t.name,
    t."serialNumber",
    t.price,
    c.name as category,
    t.status
  FROM "Tool" t
  LEFT JOIN "Category" c ON t."categoryId" = c.id
  WHERE t."companyId" = 'YOUR_COMPANY_ID'
) TO '/tmp/tools_export.csv' CSV HEADER;
```

**3. JSON через API:**
```bash
curl http://localhost:5000/api/tools -b cookies.txt > tools.json
```

---

## 🚀 Дальнейшее развитие

### В: Планируется ли UI для управления категориями?

**О:** Да! В планах:
- Страница "Категории" в админ-панели
- CRUD интерфейс для категорий
- Массовое назначение категорий
- Drag & drop для изменения категорий

Пока используйте API для управления категориями.

---

### В: Будет ли поддержка нескольких валют?

**О:** В текущей версии нет, но можно добавить:
1. Поле `currency` в модель Tool
2. Конвертер валют на фронте
3. Отчеты в разных валютах

Создайте issue на GitHub если нужна эта функция.

---

### В: Можно ли добавить подкатегории?

**О:** Да, можно расширить модель:

```prisma
model Category {
  // ... существующие поля
  parentId String?
  parent   Category? @relation("SubCategories", fields: [parentId], references: [id])
  children Category[] @relation("SubCategories")
}
```

Но рекомендуется начать с простой структуры.

---

## 📞 Поддержка

### В: Где получить помощь?

**О:** Несколько вариантов:

1. **Документация:**
   - [MIGRATION_GUIDE.md](./MIGRATION_GUIDE.md) - полная инструкция
   - [API_CATEGORIES.md](./API_CATEGORIES.md) - API документация
   - [QUICK_START.md](./QUICK_START.md) - быстрый старт

2. **Логи:**
   ```bash
   # Backend логи
   cat /tmp/backend.log
   
   # Database логи
   docker logs toolmanager_postgres
   
   # Frontend - консоль браузера (F12)
   ```

3. **GitHub Issues:**
   Создайте issue с описанием проблемы

4. **Community:**
   Задайте вопрос в Discussions

---

**Последнее обновление:** 06.11.2024  
**Версия:** 1.0  
**Автор:** ToolManager Development Team

---

💡 **Совет:** Добавьте этот файл в закладки для быстрого доступа к ответам!
