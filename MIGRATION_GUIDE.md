# 📦 Руководство по миграции - Добавление системы складов

## Описание изменений

В систему ToolManager добавлена функциональность управления складами:
- Новая сущность `Warehouse` (склад)
- Права доступа для управления складами
- Привязка инструментов к складам
- Возможность передавать инструменты как сотрудникам, так и на склады
- Страница управления складами в интерфейсе

## Инструкция по запуску миграции

### ⚠️ ВАЖНО: Сделайте резервную копию базы данных перед миграцией!

```bash
# Пример для PostgreSQL
pg_dump -h localhost -U your_user -d toolmanager > backup_$(date +%Y%m%d_%H%M%S).sql
```

### Шаг 1: Остановите приложение

```bash
# Остановите backend и frontend серверы
# Если они запущены в фоне, найдите и завершите процессы
```

### Шаг 2: Обновите код

Убедитесь, что все файлы обновлены из репозитория:
```bash
cd /home/engine/project
git pull origin feat-warehouses-page-add-create-assign-transfer-permissions-migrations
```

### Шаг 3: Установите зависимости (если нужно)

```bash
# Backend
cd backend
npm install

# Frontend
cd ../frontend
npm install
```

### Шаг 4: Обновите схему базы данных

```bash
cd backend

# Синхронизируйте схему Prisma с базой данных
npx prisma db push

# Сгенерируйте Prisma Client с новыми моделями
npx prisma generate
```

### Шаг 5: Запустите миграцию данных

Этот скрипт создаст дефолтные склады для всех существующих компаний и привяжет к ним инструменты:

```bash
cd backend
node migrations/create-default-warehouses.js
```

**Что делает скрипт миграции:**
1. Находит все компании в системе
2. Для каждой компании без складов создает "Основной склад" (с флагом `isDefault`)
3. Привязывает все инструменты, которые находятся "на складе" (без владельца), к дефолтному складу
4. Для компаний, у которых уже есть склады, проверяет наличие дефолтного склада

### Шаг 6: Проверьте результаты миграции

Откройте Prisma Studio для проверки данных:
```bash
cd backend
npx prisma studio
```

Проверьте:
- ✅ У каждой компании есть хотя бы один склад
- ✅ У каждой компании есть склад с флагом `isDefault = true`
- ✅ Инструменты без владельца имеют `warehouseId`

### Шаг 7: Обновите права доступа для ролей

**Для роли "Босс" права уже добавлены автоматически.**

Для остальных ролей нужно вручную назначить права через интерфейс или базу данных:

**Новые права доступа:**
- `WAREHOUSE_CREATE` - Создавать склад
- `WAREHOUSE_UPDATE` - Редактировать склад
- `WAREHOUSE_DELETE` - Удалять склад
- `WAREHOUSE_READ` - Видеть список складов

**Через Prisma Studio:**
1. Откройте модель `Role`
2. Найдите роль, которой нужно дать права
3. Отредактируйте поле `permissions`, добавив нужные права

**Через SQL (пример для PostgreSQL):**
```sql
-- Добавить все права на склады для конкретной роли
UPDATE "Role" 
SET permissions = array_cat(
  permissions, 
  ARRAY['WAREHOUSE_CREATE', 'WAREHOUSE_UPDATE', 'WAREHOUSE_DELETE', 'WAREHOUSE_READ']::text[]
)
WHERE name = 'Менеджер' AND "companyId" = 'your-company-id';
```

### Шаг 8: Запустите приложение

```bash
# Terminal 1 - Backend
cd backend
npm run dev

# Terminal 2 - Frontend
cd frontend
npm run dev
```

### Шаг 9: Проверьте работу в интерфейсе

1. Войдите в систему как Босс
2. Откройте страницу "Склады" в навигации
3. Проверьте, что отображается "Основной склад"
4. Попробуйте создать новый склад
5. Откройте страницу "Инструменты"
6. Попробуйте создать инструмент с выбором склада
7. Попробуйте передать инструмент на другой склад

## Откат миграции (если что-то пошло не так)

### Вариант 1: Восстановление из бэкапа

```bash
# Восстановите базу данных из бэкапа
psql -h localhost -U your_user -d toolmanager < backup_YYYYMMDD_HHMMSS.sql
```

### Вариант 2: Ручной откат через SQL

```sql
-- Удалить склады
DELETE FROM "Warehouse";

-- Убрать связи инструментов со складами
UPDATE "Tool" SET "warehouseId" = NULL;

-- Удалить новые поля из истории
UPDATE "ToolHistory" 
SET "fromWarehouseId" = NULL, "toWarehouseId" = NULL;
```

Затем откатите схему Prisma к предыдущей версии и выполните:
```bash
cd backend
npx prisma db push
npx prisma generate
```

## Проверка после миграции

### Проверка через API

```bash
# Получить список складов
curl -X GET http://localhost:5000/api/warehouses \
  -H "Cookie: token=YOUR_TOKEN"

# Получить список инструментов (должны иметь поле warehouse)
curl -X GET http://localhost:5000/api/tools \
  -H "Cookie: token=YOUR_TOKEN"
```

### Тестовые сценарии

1. **Создание инструмента**
   - Создайте инструмент через интерфейс
   - Убедитесь, что он привязан к дефолтному складу

2. **Передача инструмента**
   - Выдайте инструмент сотруднику
   - Убедитесь, что `warehouseId` стал `null`, а `currentUserId` заполнен

3. **Возврат на склад**
   - Верните инструмент на склад
   - Убедитесь, что можно выбрать конкретный склад

4. **Передача между складами**
   - Передайте инструмент с одного склада на другой
   - Проверьте историю перемещений

## Поддержка

Если возникли проблемы с миграцией:
1. Проверьте логи backend сервера
2. Проверьте данные в Prisma Studio
3. Убедитесь, что все шаги выполнены в правильном порядке
4. При необходимости откатите изменения и попробуйте снова

## Структура новых данных

### Модель Warehouse
```prisma
model Warehouse {
  id        String   @id @default(cuid())
  name      String
  companyId String
  company   Company  @relation(...)
  isDefault Boolean  @default(false)
  createdAt DateTime @default(now())
  tools     Tool[]
  historyFrom ToolHistory[] @relation("HistoryFromWarehouse")
  historyTo   ToolHistory[] @relation("HistoryToWarehouse")
  @@unique([companyId, name])
}
```

### Обновления в модели Tool
- Добавлено поле `warehouseId` (связь со складом)
- Инструмент может быть либо у пользователя (`currentUserId`), либо на складе (`warehouseId`)

### Обновления в модели ToolHistory
- Добавлены поля `fromWarehouseId` и `toWarehouseId`
- История теперь отслеживает перемещения между складами

## Важные замечания

1. **Изоляция данных**: Все склады привязаны к компании (мультитенантность)
2. **Дефолтный склад**: У каждой компании должен быть один дефолтный склад
3. **Нельзя удалить дефолтный склад**: Система не позволит удалить склад с флагом `isDefault`
4. **Нельзя удалить склад с инструментами**: Сначала нужно переместить инструменты на другой склад
5. **Права доступа**: Не забудьте назначить права на работу со складами нужным ролям
