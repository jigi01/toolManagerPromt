# 🎯 ПРОЧИТАЙ СНАЧАЛА!

## Что было исправлено

Все настроено для работы мобильного приложения с backend по IP `192.168.0.128` на порту `5001`.

## ⚡ Быстрый старт

### 1️⃣ Запустите backend
```bash
cd backend
npm run dev
```

Должны увидеть:
```
🚀 Сервер запущен на порту 5001 и доступен на всех сетевых интерфейсах
📱 Мобильное приложение: http://192.168.0.128:5001/api
💻 Веб-приложение: http://localhost:5001/api
```

### 2️⃣ Проверьте backend по IP
```bash
curl http://192.168.0.128:5001/api/health
```

**✅ Работает?** → Переходите к шагу 3

**❌ Не работает?** → Откройте порт в firewall:

#### Windows (PowerShell от администратора):
```powershell
New-NetFirewallRule -DisplayName "ToolManager Backend" -Direction Inbound -LocalPort 5001 -Protocol TCP -Action Allow
```

#### Linux:
```bash
sudo ufw allow 5001/tcp
```

### 3️⃣ Запустите mobile
```bash
cd mobile
pnpm install
pnpm start
```

Scan QR код в Expo Go и попробуйте залогиниться.

## 📚 Документация

- **`QUICK_FIX_SUMMARY.md`** - Краткая сводка всех изменений
- **`TEST_INSTRUCTIONS.md`** - Подробные инструкции по тестированию
- **`MOBILE_SETUP.md`** - Полная документация по настройке

## 🔍 Проверка конфигурации

Все должно быть настроено на порт **5001**:

```bash
# Backend
grep PORT backend/.env
# Ожидается: PORT=5001

# Mobile
grep EXPO_PUBLIC_API_URL mobile/.env
# Ожидается: EXPO_PUBLIC_API_URL=http://192.168.0.128:5001/api
```

## ❗ Если что-то не работает

1. **Backend не доступен по IP** → Проверьте firewall (см. шаг 2)
2. **CORS ошибка** → Перезапустите backend
3. **Mobile не подключается** → Убедитесь, что телефон и ПК в одной Wi-Fi сети

## 📞 Что изменилось

### До:
- ❌ Порты: 5000 и 5001 вперемешку
- ❌ Backend слушал только localhost
- ❌ CORS не разрешал локальную сеть
- ❌ Mobile использовал localhost

### После:
- ✅ Порт: **5001 везде**
- ✅ Backend слушает `0.0.0.0` (все интерфейсы)
- ✅ CORS разрешает `192.168.x.x`
- ✅ Mobile использует IP: `192.168.0.128:5001`

---

**Готово к тестированию!** 🚀

Следуйте шагам выше и всё должно заработать.
