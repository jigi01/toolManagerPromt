# Инструкции по тестированию после изменений

## Что было исправлено

✅ Все файлы теперь используют порт **5001** (было 5000/5001 вперемешку)
✅ Backend настроен на прослушивание всех интерфейсов (0.0.0.0)
✅ CORS настроен для разрешения запросов из локальной сети (192.168.x.x)
✅ Mobile app настроен на использование IP 192.168.0.128:5001

## Проверка настроек

### 1. Проверьте backend конфигурацию

```bash
cd backend
cat .env
```

Должно быть:
```
PORT=5001
```

### 2. Проверьте mobile конфигурацию

```bash
cd mobile
cat .env
cat app.json | grep apiUrl
```

Должно быть:
- `.env`: `EXPO_PUBLIC_API_URL=http://192.168.0.128:5001/api`
- `app.json`: `"apiUrl": "http://192.168.0.128:5001/api"`

## Тестирование

### Шаг 1: Запустите backend

```bash
cd backend
npm run dev
```

Вы должны увидеть:
```
🚀 Сервер запущен на порту 5001 и доступен на всех сетевых интерфейсах
📱 Мобильное приложение: http://192.168.0.128:5001/api
💻 Веб-приложение: http://localhost:5001/api
```

### Шаг 2: Проверьте доступность по localhost

```bash
curl http://localhost:5001/api/health
```

Ожидаемый результат:
```json
{"status":"OK","message":"ToolManager API is running"}
```

### Шаг 3: Проверьте доступность по IP (ВАЖНО!)

```bash
curl http://192.168.0.128:5001/api/health
```

**Если получаете ошибку соединения:**

#### Windows (Firewall):
```powershell
# Откройте PowerShell от имени администратора
New-NetFirewallRule -DisplayName "ToolManager Backend" -Direction Inbound -LocalPort 5001 -Protocol TCP -Action Allow
```

Или вручную:
1. Панель управления → Брандмауэр Windows → Дополнительные параметры
2. Правила для входящих подключений → Создать правило
3. Тип: Порт → TCP → 5001
4. Действие: Разрешить подключение

#### macOS (Firewall):
```bash
# Обычно не требуется, но если включен firewall:
# System Preferences → Security & Privacy → Firewall → Firewall Options
# Добавьте Node.js в список разрешенных приложений
```

#### Linux (UFW):
```bash
sudo ufw allow 5001/tcp
sudo ufw reload
```

### Шаг 4: Проверьте CORS для mobile origin

```bash
curl -X OPTIONS http://192.168.0.128:5001/api/auth/login \
  -H "Origin: http://localhost:8081" \
  -H "Access-Control-Request-Method: POST" \
  -H "Access-Control-Request-Headers: Content-Type" \
  -v
```

Должны увидеть в заголовках ответа:
```
< Access-Control-Allow-Origin: http://localhost:8081
< Access-Control-Allow-Credentials: true
```

### Шаг 5: Запустите mobile приложение

```bash
cd mobile
pnpm install
pnpm start
```

Scan QR код на телефоне через Expo Go.

### Шаг 6: Попробуйте залогиниться

В мобильном приложении попробуйте:
1. Зарегистрироваться
2. Войти

**Проверьте консоль на ошибки:**
- ❌ "Network request failed" → Backend недоступен по IP
- ❌ "CORS policy" → Проблема с CORS настройками
- ✅ Успешный вход → Всё работает!

## Решение распространенных проблем

### Проблема: curl к IP не работает (connection timeout/refused)

**Причина:** Firewall блокирует порт или backend не слушает на 0.0.0.0

**Решение:**
1. Убедитесь, что в логах backend написано "доступен на всех сетевых интерфейсах"
2. Добавьте правило в firewall (см. Шаг 3 выше)
3. Проверьте, что используете правильный IP:
   ```bash
   # Windows
   ipconfig
   
   # macOS/Linux
   ifconfig | grep "inet "
   ```

### Проблема: CORS ошибка при логине с localhost:8081

**Причина:** CORS не пропускает preflight запрос

**Решение:**
Проверьте в `backend/src/server.js`:
```javascript
const allowedOrigins = [
  'http://localhost:5173',
  'http://localhost:8081',  // ← Должно быть
  'http://192.168.0.128:8081',
];
```

И также должна быть логика для 192.168.x.x:
```javascript
if (origin && origin.startsWith('http://192.168.')) {
  callback(null, true);
}
```

### Проблема: Mobile app не может подключиться

**Причина:** Неправильный API URL или устройство не в той же сети

**Решение:**
1. Убедитесь, что телефон и компьютер в одной Wi-Fi сети
2. Проверьте IP в mobile/.env и app.json
3. После изменения конфигурации:
   ```bash
   # Остановите Expo (Ctrl+C)
   # Очистите кэш
   pnpm start -c
   ```

## Чеклист готовности

- [ ] Backend запускается на порту 5001
- [ ] `curl http://localhost:5001/api/health` работает
- [ ] `curl http://192.168.0.128:5001/api/health` работает
- [ ] CORS preflight запрос проходит успешно
- [ ] Mobile app подключается к backend
- [ ] Можно залогиниться в mobile app

Если все пункты выполнены - всё работает! 🎉
