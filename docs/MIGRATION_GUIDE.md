# 📋 Руководство по применению изменений: Цена и Категории

## 🎯 Что было добавлено

В систему добавлены новые функции:
- **Цена инструмента** - поле для указания стоимости (необязательное)
- **Категории инструментов** - возможность группировать инструменты по категориям

## ⚠️ Важно перед началом

**ОБЯЗАТЕЛЬНО СДЕЛАЙТЕ РЕЗЕРВНУЮ КОПИЮ БАЗЫ ДАННЫХ!**

```bash
# Сохранить дамп базы данных
docker exec toolmanager_postgres pg_dump -U postgres toolmanager > backup_$(date +%Y%m%d_%H%M%S).sql
```

## 📦 Шаг 1: Обновление зависимостей

### Backend
```bash
cd backend

# Удалить старые зависимости и установить заново
rm -rf node_modules package-lock.json
npm install

# Или если используете yarn
rm -rf node_modules yarn.lock
yarn install
```

### Frontend
```bash
cd frontend

# Установить новую зависимость для QR-кодов (уже добавлена)
pnpm install

# Или
npm install
# Или
yarn install
```

## 🗄️ Шаг 2: Применение миграции базы данных

### Проверка состояния базы данных

```bash
cd backend

# Убедитесь, что база данных запущена
docker compose up -d

# Проверьте подключение
npx prisma db pull --schema=prisma/schema.prisma
```

### Применение миграции

Миграция уже создана и находится в `backend/prisma/migrations/20251106192845_add_price_and_categories/`

```bash
cd backend

# Применить миграцию
npx prisma migrate deploy

# Или для разработки (с генерацией клиента)
npx prisma migrate dev

# Сгенерировать Prisma Client (если еще не сделано)
npx prisma generate
```

### Проверка миграции

```bash
# Откройте Prisma Studio для визуальной проверки
npx prisma studio

# Или проверьте через psql
docker exec -it toolmanager_postgres psql -U postgres -d toolmanager -c "\d \"Tool\""
docker exec -it toolmanager_postgres psql -U postgres -d toolmanager -c "\d \"Category\""
```

Должны появиться:
- В таблице `Tool`: поля `price` (Decimal) и `categoryId` (String)
- Новая таблица `Category` с полями: `id`, `name`, `companyId`, `createdAt`

## 🔧 Шаг 3: Настройка окружения

### Backend

Убедитесь, что файл `.env` существует:

```bash
cd backend

# Если .env не существует, создайте из примера
cp .env.example .env

# Проверьте настройки
cat .env
```

Содержимое должно быть примерно таким:
```env
DATABASE_URL="postgresql://postgres:zhiga@localhost:5432/toolmanager?schema=public"
JWT_SECRET="your_super_secret_jwt_key_change_this_in_production"
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173
```

## 🚀 Шаг 4: Запуск приложения

### Запуск базы данных

```bash
# В корневой директории проекта
docker compose up -d

# Проверьте, что контейнер запущен
docker ps | grep toolmanager_postgres
```

### Запуск Backend

```bash
cd backend

# Режим разработки (с автоперезагрузкой)
npm run dev

# Или обычный запуск
npm start
```

Backend должен запуститься на порту 5000. Проверьте:
```bash
curl http://localhost:5000/api/health
# Должно вернуть: {"status":"OK","message":"ToolManager API is running"}
```

### Запуск Frontend

```bash
cd frontend

# Режим разработки
pnpm dev

# Или
npm run dev
# Или
yarn dev
```

Frontend должен запуститься на порту 5173. Откройте http://localhost:5173

## ✅ Шаг 5: Проверка работоспособности

### 1. Проверка API для категорий

```bash
# Получение категорий (требуется авторизация)
curl -X GET http://localhost:5000/api/categories \
  -H "Content-Type: application/json" \
  -b cookies.txt

# Создание категории (требуется авторизация и права TOOL_CREATE)
curl -X POST http://localhost:5000/api/categories \
  -H "Content-Type: application/json" \
  -b cookies.txt \
  -d '{"name": "Электроинструмент"}'
```

### 2. Проверка в UI

1. Войдите в систему
2. Перейдите на страницу "Инструменты"
3. Нажмите "Добавить Инструмент"
4. Проверьте, что появились новые поля:
   - **Категория** (выпадающий список)
   - **Цена** (числовое поле)
5. Создайте тестовый инструмент с ценой и категорией
6. Проверьте отображение в карточках и таблице

### 3. Проверка существующих данных

Все существующие инструменты:
- ✅ Останутся в базе без изменений
- ✅ Будут иметь `price = null` (не отображается)
- ✅ Будут иметь `categoryId = null` (без категории)
- ✅ Продолжат работать как раньше

## 🔄 Откат изменений (если что-то пошло не так)

### Откат миграции базы данных

```bash
cd backend

# Откатить последнюю миграцию
npx prisma migrate resolve --rolled-back 20251106192845_add_price_and_categories

# Восстановить из резервной копии
docker exec -i toolmanager_postgres psql -U postgres toolmanager < backup_ДАТА.sql
```

### Откат кода

```bash
# Вернуться к предыдущему коммиту
git checkout HEAD~1

# Или откатить конкретные файлы
git checkout HEAD~1 -- backend/prisma/schema.prisma
git checkout HEAD~1 -- backend/src/services/tool.service.js
```

## 📊 Миграция существующих данных (опционально)

Если хотите добавить категории для существующих инструментов:

```sql
-- Создайте базовые категории для вашей компании
INSERT INTO "Category" (id, name, "companyId", "createdAt") 
VALUES 
  (gen_random_uuid(), 'Электроинструмент', 'YOUR_COMPANY_ID', NOW()),
  (gen_random_uuid(), 'Ручной инструмент', 'YOUR_COMPANY_ID', NOW()),
  (gen_random_uuid(), 'Измерительный', 'YOUR_COMPANY_ID', NOW());

-- Обновите инструменты (пример)
UPDATE "Tool" 
SET "categoryId" = (SELECT id FROM "Category" WHERE name = 'Электроинструмент' LIMIT 1)
WHERE name ILIKE '%дрель%' OR name ILIKE '%перфоратор%';
```

## 🐛 Решение проблем

### Проблема: Ошибка при миграции

```bash
# Сбросить состояние миграции
npx prisma migrate reset

# Применить все миграции заново
npx prisma migrate deploy
```

### Проблема: Backend не запускается (bcrypt ошибка)

```bash
cd backend
rm -rf node_modules package-lock.json
npm install
npm rebuild bcrypt
```

### Проблема: Категории не загружаются на фронте

1. Проверьте, что backend запущен и доступен
2. Проверьте консоль браузера (F12)
3. Проверьте, что у пользователя есть права `TOOL_READ`

```bash
# Проверка API
curl http://localhost:5000/api/categories -H "Cookie: YOUR_COOKIE"
```

### Проблема: Цена некорректно отображается

Цена хранится как `Decimal` в базе и должна быть преобразована в число:
```javascript
// Правильно
parseFloat(tool.price).toFixed(2)

// Неправильно
tool.price.toFixed(2) // Ошибка если price это Decimal объект
```

## 📝 Changelog

### Добавлено

**Backend:**
- ✅ Поле `price` (Decimal, необязательное) в модели `Tool`
- ✅ Модель `Category` с уникальным ограничением `[name, companyId]`
- ✅ Связь `Tool.category` с `onDelete: SetNull`
- ✅ Связь `Company.categories` с `onDelete: Cascade`
- ✅ API endpoints: `/api/categories` (GET, POST, PUT, DELETE)
- ✅ Сервис `category.service.js` со всеми CRUD операциями
- ✅ Контроллер `category.controller.js` с валидацией
- ✅ Фильтр по категории в `/api/tools?categoryId=...`

**Frontend:**
- ✅ Поля "Категория" и "Цена" в форме создания инструмента
- ✅ Поля "Категория" и "Цена" в форме редактирования инструмента
- ✅ Отображение категории и цены в карточках инструментов
- ✅ Отображение категории и цены в таблице инструментов
- ✅ Отображение категории и цены на странице деталей инструмента

### Изменено

- Функция `createTool` теперь принимает `price` и `categoryId`
- Функция `updateTool` теперь проверяет категорию перед обновлением
- Все запросы инструментов включают `category` в ответе

### Безопасность

- ✅ Категории изолированы по компаниям
- ✅ Проверка прав доступа для всех операций с категориями
- ✅ Валидация данных на уровне контроллера
- ✅ При удалении категории, инструменты не удаляются (categoryId = null)

## 🆘 Поддержка

Если возникли проблемы:

1. Проверьте логи backend: `cat /tmp/backend.log`
2. Проверьте консоль браузера (F12 → Console)
3. Проверьте логи базы данных: `docker logs toolmanager_postgres`
4. Восстановите из резервной копии если нужно

## ✨ Следующие шаги

После успешного применения изменений рекомендуется:

1. **Создать базовые категории** для вашей компании через UI
2. **Обновить существующие инструменты** - добавить цены и категории
3. **Настроить права доступа** если нужны отдельные права для управления категориями
4. **Создать резервную копию** обновленной базы данных

---

**Дата создания:** 06.11.2024  
**Версия:** 1.0  
**Автор:** ToolManager Development Team
