# 🚀 Следующие шаги для тестирования

## Что исправлено

✅ **IP обновлен**: 192.168.0.128 → **192.168.0.191**
✅ **Expo Web**: теперь использует `localhost:5001` с cookies
✅ **Mobile (iOS/Android)**: использует `192.168.0.191:5001` с Bearer токенами
✅ **CORS**: настроен для обеих платформ

## Что нужно сделать

### 1. Перезапустите backend
```bash
# Остановите backend (Ctrl+C)
cd backend
npm run dev
```

Вы должны увидеть:
```
🚀 Сервер запущен на порту 5001 и доступен на всех сетевых интерфейсах
📱 Мобильное приложение: http://192.168.0.191:5001/api
💻 Веб-приложение: http://localhost:5001/api
```

### 2. Перезапустите Expo с очисткой кэша
```bash
# Остановите Expo (Ctrl+C)
cd mobile
pnpm start -c
```

Флаг `-c` очистит кэш и применит новые настройки.

### 3. Протестируйте Expo Web
В терминале Expo нажмите `w` или откройте браузер:
```
http://localhost:8081
```

Попробуйте:
1. Зарегистрироваться (если нет аккаунта)
2. Залогиниться

**Ожидаемый результат**: ✅ Успешный вход, без ошибок CORS

### 4. Протестируйте на телефоне
1. Scan QR код в Expo Go
2. Попробуйте залогиниться

**Ожидаемый результат**: ✅ Успешный вход

## Что изменилось в коде

### mobile/services/api.ts
```typescript
// БЫЛО:
const API_URL = 'http://192.168.0.128:5001/api';
withCredentials: false

// СТАЛО:
const getApiUrl = () => {
  if (Platform.OS === 'web') {
    return 'http://localhost:5001/api';  // Web → localhost
  }
  return 'http://192.168.0.191:5001/api'; // Mobile → IP
};

withCredentials: Platform.OS === 'web'  // Web → true, Mobile → false
```

### Почему это важно
- **Web (localhost:8081)**: Должен подключаться к backend через localhost и использовать cookies
- **Mobile (iOS/Android)**: Должен подключаться через IP и использовать Bearer токены
- Раньше всё было настроено только для Mobile, теперь работает для обоих

## Если что-то не работает

### Expo Web - CORS ошибка
```bash
# Проверьте, что backend запущен
curl http://localhost:5001/api/health

# Должен вернуть:
# {"status":"OK","message":"ToolManager API is running"}
```

Если не работает - backend не запущен или порт неправильный.

### Mobile - Network request failed
```bash
# Проверьте, что backend доступен по IP
curl http://192.168.0.191:5001/api/health

# Если не работает - проблема с firewall
```

**Решение для Windows:**
```powershell
New-NetFirewallRule -DisplayName "ToolManager" -Direction Inbound -LocalPort 5001 -Protocol TCP -Action Allow
```

### После изменений не применяются
```bash
# Очистите кэш Expo
cd mobile
pnpm start -c

# Или удалите .expo папку
rm -rf .expo
pnpm start
```

## Проверка что всё работает

✅ Expo Web (w):
- [ ] Открывается http://localhost:8081
- [ ] Можно зарегистрироваться
- [ ] Можно залогиниться
- [ ] Нет CORS ошибок в консоли

✅ Mobile (Expo Go):
- [ ] QR код сканируется
- [ ] Приложение загружается
- [ ] Можно залогиниться
- [ ] Данные загружаются

Если все пункты отмечены - **ВСЁ РАБОТАЕТ!** 🎉

## Дополнительная информация

Читайте:
- `FIXED_WEB_AND_MOBILE.md` - Детали исправления
- `QUICK_FIX_SUMMARY.md` - Быстрая сводка
- `TEST_INSTRUCTIONS.md` - Подробные тесты
