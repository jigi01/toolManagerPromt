# ⚡ Быстрый старт: Применение изменений

## 🎯 Что нужно сделать

Добавлены **цена** и **категории** для инструментов. Для применения изменений выполните команды ниже.

## ⚠️ ВАЖНО: Резервная копия

```bash
# Сделайте резервную копию ПРЕЖДЕ ЧЕМ ПРОДОЛЖИТЬ!
docker exec toolmanager_postgres pg_dump -U postgres toolmanager > backup_$(date +%Y%m%d_%H%M%S).sql
```

## 🚀 Применение изменений (5 минут)

### 1. Обновите зависимости

```bash
# Backend
cd backend
rm -rf node_modules package-lock.json
npm install

# Frontend
cd ../frontend
pnpm install  # или npm install
```

### 2. Примените миграцию БД

```bash
# Запустите БД (если еще не запущена)
cd ..
docker compose up -d

# Примените миграцию
cd backend
npx prisma migrate deploy
npx prisma generate
```

### 3. Запустите приложение

```bash
# Backend (терминал 1)
cd backend
npm run dev

# Frontend (терминал 2)
cd frontend
pnpm dev
```

## ✅ Проверка

1. Откройте http://localhost:5173
2. Войдите в систему
3. Перейдите в "Инструменты" → "Добавить Инструмент"
4. Увидите новые поля: **Категория** и **Цена**

## 📖 Подробная документация

См. файл [MIGRATION_GUIDE.md](./MIGRATION_GUIDE.md) для полной инструкции и решения проблем.

## 🔄 Откат (если что-то пошло не так)

```bash
# Восстановите БД из копии
docker exec -i toolmanager_postgres psql -U postgres toolmanager < backup_ДАТА.sql

# Откатите код
git checkout HEAD~1
```

---

**Время выполнения:** ~5 минут  
**Сложность:** Простая  
**Риски:** Минимальные (все поля необязательные, обратная совместимость)
